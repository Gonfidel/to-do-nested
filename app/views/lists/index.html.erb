<p id="notice"><%= notice %></p>


</div class="ui middle aligned aligned grid">
  <div class="ui main container">
  <h1>Lists</h1>
<table class="ui selectable left aligned table small ">
  <thead>
    <tr class="ui selectabled black">
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lists.each do |list| %>
      <tr class="ui left aligned table gray">
        <td><%= link_to list.name, board_list_items_path(list_id: list.id), :class => "ui button primary" %></td>
        <td><%= link_to 'Back', boards_path(), :class => "ui button primary" %></td>
        <td><%= link_to 'Edit', edit_board_list_path(id: list.id), :class => "ui button primary" %></td>
        <td><%= link_to 'Destroy', board_list_path(id: list.id), method: :delete, data: { confirm: 'Are you sure?' }, :class => "ui button primary" %></td>
      </tr>

    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New List', new_board_list_path(board_id: params[:board_id]), :class => "ui button primary center" %>
  </div>
</div>